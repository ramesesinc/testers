rameses.dir=C:/RAMESES
projects.dir=${rameses.dir}/projects
platforms.dir=${rameses.dir}/platforms
platform.libs.dir=${rameses.dir}/platform-libs
osiris3.server.dir=${rameses.dir}/osiris3-server

target.dir=C:/Temp
target.app.name=etracs25
target.server.dir=${target.dir}/osiris3-server-${target.app.name}
target.services.dir=${target.server.dir}/workspace/services
target.web.dir=${target.server.dir}/workspace/web
target.apps.dir=${target.services.dir}/apps
target.app.dir=${target.apps.dir}/${target.app.name}

target.app.host=localhost:8070

lgu.type=municipality
